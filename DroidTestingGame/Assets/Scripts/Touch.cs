using UnityEngine;
using System.Collections;

public class Touch : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

		if (Input.touchCount > 0) {
			//touch controls
				

		}
	//Multitouch check
	/*	if (Input.touchCount > 0 && Input.touchCount < 2) {
			Debug.Log ("Nun befindet sich " + Input.touchCount + " Finger auf den Display.");
			return;
		} else if (Input.touchCount > 1) {
			Debug.Log ("Nun befinden sich " + Input.touchCount + " Finger auf dem Display.");
			return;
		}
	*/}
}
